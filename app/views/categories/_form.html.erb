  <%= form_with model: category do |form|%>
    <p><%= form.label :name, "Назва категоріЇ" %> </p>
    <p><%= form.text_field :name, class: 'form-text-area' %></p>    

    <p class="mt"><%= form.submit "Зберегти", class: 'button button-main' %></p>
  <% end %>