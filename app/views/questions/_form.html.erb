  <%= form_with model: question do |form|%>
    <p><%= form.label :body, "Текст питання" %> </p>
    <p><%= form.text_area :body, rows: 5, class: 'form-text-area' %></p>

    <p><%= form.label :category_id, "ID категорії" %> </p>
    <p><%= form.collection_select :category_id, Category.order(:name), :id, :name, { include_blank: true }, class: 'form-text-area' %></p>

    <%# <p class="mt"><%= form.label :user_id %></p>
    <%# <p><%= form.hidden_field :user_id, value: current_user.id %>
    

    <p class="mt"><%= form.submit "Зберегти", class: 'button button-main' %></p>
  <% end %>